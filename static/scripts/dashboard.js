window.addEventListener("load", () => {
  console.log("Dashboard loaded successfully!");
  // In real integration, fetch data from backend and visualize
  // Example: fetch('/api/expenses').then(res => res.json()).then(updateCharts);
});
